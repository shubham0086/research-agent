# HTTP client — used for all provider calls (no SDK dependencies needed)
httpx>=0.27.0

# Content extraction
requests>=2.31.0
beautifulsoup4>=4.12.0

# Env loading
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# -----------------------------------------------------------------------
# Optional provider SDKs — only needed if you prefer SDK over direct HTTP.
# The router uses direct httpx calls and does NOT require these.
# Install only what you use.
# -----------------------------------------------------------------------
# anthropic>=0.40.0
# openai>=1.30.0
# groq>=0.11.0
# google-generativeai>=0.8.0
