# LLM providers — install whichever you use (all optional, at least one recommended)
anthropic>=0.40.0          # Anthropic Claude — set ANTHROPIC_API_KEY
openai>=1.30.0             # OpenAI GPT-4o — set OPENAI_API_KEY
groq>=0.11.0               # Groq (free tier) — set GROQ_API_KEY
google-generativeai>=0.8.0 # Google Gemini (free tier) — set GEMINI_API_KEY

# HTTP + content extraction
requests>=2.31.0
beautifulsoup4>=4.12.0

# Utilities
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
